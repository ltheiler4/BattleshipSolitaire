<Window x:Class="Battleship.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Battleship"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="300" ResizeMode="NoResize">
    <Grid>
        <Button Name="Generate" Content="Generate Quiz" Margin="50,50,50,270" Command="{Binding RelayCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Self}}"></Button>
        <Button Name="Config" Content="Config" Margin="50,150,50,170" Command="{Binding RelayCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Self}}"></Button>
    </Grid>
</Window>
